# Codebase Review — metaseed-hub 0.33.0 (260813)

Per-file review of `src/metaseed_hub` plus the alembic migrations and scripts — 10 module groups, one reviewer each, then an adversarial verifier per high/medium finding. Only findings that survived verification are confirmed; the verifier's corrected severity is the one shown. 130 files reviewed, 93 raw findings, 37 confirmed after de-duplication (8 high, 30 medium, 4 low), 2 refuted, 49 lower-confidence notes in the appendix.

This supersedes the 260730 review. It records what was found; it changes nothing.

## Baseline gates

All green at the commit reviewed (`930ac92`, tag v0.33.0, metaseed 0.34.0):

| Gate | Result |
|---|---|
| `ruff check src tests` | pass |
| `vulture src/ --min-confidence=80` | clean |
| `mypy src/metaseed_hub` | pass (88 files) |
| File size (≤1000 lines) | 0 violations |
| TODO/FIXME markers | 0 |
| Suite (`-m "not selenium"`) | 912 passed |

## Summary

By category: correctness 24, consistency 7, design 6, dead-code 3, typing 1, docstring 1.

**Remediation status:** the security and account-lifecycle findings shipped in **0.33.1** (9 findings); the silent-data-loss, draft-lifecycle, and ontology-outage findings shipped in **0.34.0** (9 findings). Each is marked in place. The remaining medium/low findings and the appendix are tracked as backlog.

The eight high findings cluster into three stories, and none of them is a crash — each is the hub doing something *quietly* wrong for a multi-user product:

1. **Authorization and account lifecycle.** Dataset VIEWER members can edit and delete shared datasets — `EDIT_ROLES` exists and is enforced nowhere on the content-mutation paths. Account deletion is blocked forever by items the UI no longer shows (soft-deleted datasets, withdrawn specs), while GDPR erasure leaves the per-user Tenant row and the encrypted SEEK API key behind. A soft-deleted user's access token still authenticates.
2. **Silent data loss on the write paths.** Three import/example routes bypass the unloadable-node refusal and silently drop skipped nodes on save; truncated display values (`…`-suffixed) are rendered into editable inputs and saved back on blur; primitive-list rows keep stale indices after a delete so the next edit hits the wrong item; MCP `save_dataset` skips the canonical write-path invariants.
3. **Draft lifecycle integrity.** Publishing a draft deletes it and orphans every dataset built on it (delete refuses in exactly this situation; publish does not); a bad version string typed into profile metadata is persisted unvalidated and bricks the draft on next load.

Recurring themes below high severity: the **fork rule strained twice** (EntityService reimplements the dataset load path with drifting semantics; hand-built row HTML forks the inline-table partial and has already drifted); **three-outcome checking broken again at an edge** (the hub MCP ontology tool reports an OLS outage as 'term does not exist' — the same shape as metaseed's own high finding); **REST API diverges from the UI** (ignores DatasetMember sharing; imports tenancy helpers from the UI layer); and **two alembic downgrades that cannot run** (`drop_constraint(None)`).

## Confirmed findings

### High (8)

#### `repositories/account.py:70` — Soft-deleted datasets and withdrawn specs permanently block account deletion

**Status: fixed in 0.33.1.**

datasets_needing_new_owner and specs_needing_new_owner never filter deleted_at. The UI, REST API, and MCP all delete datasets via dataset.soft_delete() (ui/routes/dataset/crud.py:703, api/datasets.py:232, mcp/__init__.py:665), and unpublishing a spec is spec.soft_delete() (ui/spec_builder/access.py:637) - the DatasetMember/SpecMember rows survive in both cases. A user who is sole owner of a dataset they already deleted, or of a spec they withdrew, is then blocked by AccountDeletionBlockedError over an item the UI no longer shows (every list query filters deleted_at IS NULL), so they are told to 'reassign or delete' something they cannot see or act on. Account deletion becomes impossible through the UI. tests/test_account_deletion.py has no soft-delete case (grep for soft_delete/deleted_at in that file returns nothing).

*Verifier:* Confirmed from code. repositories/account.py filters neither the membership queries nor the session.get(Dataset/Spec, ...) fetches by deleted_at, and no global SQLAlchemy soft-delete filter (with_loader_criteria/do_orm_execute) exists, so soft-deleted datasets/specs are returned as blockers. All three dataset delete paths (ui/routes/dataset/crud.py:703, api/datasets.py:232, mcp/__init__.py:665) and spec withdrawal (ui/spec_builder/access.py:637) only call soft_delete(), leaving DatasetMember/SpecMember rows in place; no purge job removes them later. The user cannot resolve the block: UI dataset access requires deleted_at IS NULL (ui/dependencies.py:277) so the item is invisible, remove_member raises LastOwnerError for a sole owner, restore is admin-only, and there is no user-facing hard delete. The blocked route (ui/routes/auth.py:419-422) just redirects with error=owned_datasets. The module docstring's own escape hatch ("reassign or delete it") is broken because the only available delete is soft. tests/test_account_deletion.py has no soft_delete/deleted_at coverage, as claimed. Severity high stands: GDPR account erasure is permanently blocked by the ordinary sequence of deleting one's own solely-owned dataset (or withdrawing a spec) before deleting the account.

**Fix:** Exclude soft-deleted rows: after session.get(Dataset, ...), skip when dataset.deleted_at is not None (same for Spec), or join the parent table with deleted_at IS NULL in the membership query. Add a red-first test: sole-owned soft-deleted dataset must not block delete_account.

#### `repositories/account.py:140` — GDPR erasure leaves the per-user Tenant row and SeekConnection (encrypted SEEK API key) behind

**Status: fixed in 0.33.1.**

The module docstring claims deleting the user row 'removes all personal data by cascade', but delete_account only deletes the User. Hub tenants are per user: ensure_tenant_and_user (ui/dependencies.py:180) creates the tenant with name=user.name or the email local part, so the tenant row itself carries personal data and survives. SeekConnection is keyed by tenant_id, not user_id (models/operations.py:46), and its own docstring says it is one user's connection holding their encrypted SEEK API key, URL, and last_error - none of which is removed on account deletion. tests/test_account_deletion.py never touches SeekConnection or tenant cleanup.

*Verifier:* Confirmed from the code. delete_account (src/metaseed_hub/repositories/account.py:162) deletes only the User row. The FK topology makes the docstring's "removes all personal data by cascade" false for two tables: (1) Tenant — users.tenant_id cascades tenant→user, not user→tenant, and Tenant has no FK anywhere, so the per-user tenant row survives; ensure_tenant_and_user (ui/dependencies.py:180-183) creates it with name=user.name or the email local part, i.e. personal data. (2) SeekConnection — its only FK is tenant_id→tenants.id (models/operations.py:46-48), no user FK, no ondelete, so the encrypted SEEK API key, URL, and last_error survive erasure. The sole caller (ui/routes/auth.py:417-423) does no extra cleanup, and tests/test_account_deletion.py never references SeekConnection or tenant deletion. Worse, tenant lookup by keycloak-derived slug means a returning identity is re-attached to the old tenant and its SeekConnection, proving nothing was erased. High severity stands: an explicit GDPR-erasure feature retains the subject's name and a third-party credential indefinitely.

**Fix:** In delete_account, also delete the user's SeekConnection and either delete the now-empty per-user tenant or anonymize its name when co-owned content must survive under it. Cover with tests.

#### `ui/dependencies.py:361` — Dataset VIEWER members can edit and delete shared datasets

**Status: fixed in 0.33.1.**

Every dataset content mutation authorizes via get_dataset_for_user, which grants access to ANY DatasetMember regardless of role. No dataset mutation route checks Role.VIEWER vs EDITOR: get_dataset_state_for_mutation (used by all table.py cell/row edits), entity.py create/update/delete, crud.py dataset_delete, editor.py dataset_import_into_existing, versions.py restore_dataset_version. sharing.py defines EDIT_ROLES = frozenset({Role.OWNER, Role.EDITOR}) with the comment 'Roles that may change the content of a resource', and the sharing panel offers VIEWER for datasets, but EDIT_ROLES is only enforced for spec drafts (spec_builder/access.py require_edit_role gates every mutating method) and published specs. A user shared on a dataset as viewer can therefore rewrite or soft-delete it. require_dataset_owner only gates membership management, not content.

*Verifier:* Confirmed by reading the code. get_dataset_for_user (src/metaseed_hub/ui/dependencies.py ~254-305) returns the dataset for any DatasetMember with no role filter; get_dataset_state_for_mutation (line ~361) adds only auth+CSRF and is used by all table.py cell/row mutation routes. entity.py create/delete, crud.py dataset_delete (line 674, soft-delete), import-source, load-example, editor.py mutations, and versions.py restore_dataset_version (line 280) all authorize solely via get_dataset_for_user. Role.VIEWER is offered for datasets in the sharing panel (ui/routes/sharing.py, roles=list(Role), default VIEWER), and EDIT_ROLES from sharing.py ("Roles that may change the content of a resource") is enforced only in ui/spec_builder/access.py for spec drafts. No role check exists anywhere in the dataset content mutation path (grep for role/viewer in EntityService and dataset routes returns nothing), and tests only cover require_dataset_owner denying viewers membership-management, not content mutation. A viewer-role member can therefore edit and soft-delete a shared dataset server-side. High severity is correct for this authorization bypass.

**Fix:** Add a role check to the dataset mutation path mirroring spec_builder's require_edit_role: in get_dataset_state_for_mutation and each mutating dataset route, resolve the member's role and refuse VIEWER with 403; add a gate test like tests/test_spec_builder_roles.py for datasets.

#### `ui/helpers/tables.py:241` — Truncated display values are rendered into editable inputs and saved back, corrupting data

**Status: fixed in 0.34.0.**

_build_entity_list_table truncates cell values for display: `if isinstance(val, str) and len(val) > 50: val = val[:47] + "..."`. But templates/partials/inline_table.html renders that same row value into an editable `<input ... value="{{ row.get(col, '') }}" hx-post=".../table/{{ row._node_id }}/cell" hx-trigger="change, blur">`. The /cell route (ui/routes/table.py:416 update_table_cell) writes whatever the form posts into the entity and calls save_dataset_state. So a user who focuses a cell holding a >50-char value and tabs away (blur fires even without a change) persists the truncated "..."-suffixed string over the real stored value. Note _build_single_entity_table does NOT truncate, so the two table builders are also inconsistent.

*Verifier:* Verified end to end. tables.py:241-242 truncates string values >50 chars to a 47-char prefix plus "..." inside _build_entity_list_table, and that truncated value is what lands in row_data. build_inline_tables (tables.py:390) feeds this into inline_tables, rendered by templates/partials/inline_table.html via entity_form.html:305. In the entity-list branch (inline_table.html:85-101) the truncated value is rendered as the input's value attribute with hx-post to /hub/datasets/{id}/table/{node_id}/cell and hx-trigger="change, blur" — no `changed` modifier, so htmx posts on every blur even without an edit. hub.js:271-291 focuses and selects the input when the cell is clicked, so a click-in/click-out fires blur. The route update_table_cell (ui/routes/table.py:416-472) writes the posted string into the entity via model_construct, calls state.update_node and save_dataset_state — no length check or old-value comparison. Therefore clicking a cell holding a >50-char value and clicking away silently overwrites the stored value with the "..."-suffixed truncation. Also confirmed _build_single_entity_table (tables.py:184-192) does not truncate, so the inconsistency claim holds. High severity is appropriate: silent persistent data corruption from a view-only interaction.

**Fix:** Never truncate the value that feeds the input. Either pass the full value to the template and truncate only in the display span (CSS text-overflow or a separate display key), or stop truncating in _build_entity_list_table entirely, matching _build_single_entity_table.

#### `ui/routes/dataset/editor.py:762` — Import/example mutation routes bypass the unloadable-node refusal and silently delete skipped nodes

**Status: fixed in 0.34.0.**

ensure_dataset_facade ALWAYS loads permissively (it passes its internal `report` callback to client.load even when the caller supplies no on_skip), so nodes the profile cannot place are dropped from the facade. get_dataset_state_for_mutation exists to refuse saves in that case ('covers every browser mutation at once'), but three mutation routes load-then-save without it: dataset_import_into_existing (editor.py:762 loads, :768 saves), dataset_load_example (crud.py:775), and dataset_import_source (crud.py:464 — worse, a dataset whose stored nodes ALL failed to load presents as empty, passes the 'only while empty' guard, and the import overwrites them). save_dataset_state serializes the facade, so the skipped nodes are removed from storage with no report — exactly the data-loss scenario helpers/load_report.py documents.

*Verifier:* Confirmed from code. ensure_dataset_facade (helpers/dataset_state.py) always passes an internal report callback to client.load, and metaseed's serialization.py load_node skips (returns) rather than raises when on_skip is set, so unloadable stored nodes are silently absent from the facade. get_dataset_state_for_mutation (dependencies.py:361-417) exists precisely to raise 409 via unloadable_node_refusal before any browser mutation saves, and table.py routes use it. The three named routes bypass it: dataset_import_into_existing (editor.py, loads without on_skip then save_dataset_state), dataset_load_example (crud.py ~775, same), and dataset_import_source (crud.py ~464), whose empty-dataset guard checks state.nodes_by_id — rebuilt from facade._instances, so a dataset whose stored nodes all failed to load looks empty, passes the guard, and the importer's facade replaces the stored payload on save. save_dataset_state writes serialize_tree(state) over dataset.data, removing the skipped nodes, with only a generic success notification returned. Mitigation: a DatasetVersion row preserves the prior payload, so recovery is possible via version history — but the loss is silent and the user is told the operation succeeded, which is the exact silent-shrink scenario load_report.py documents the refusal mechanism as preventing. High severity stands.

**Fix:** In these three routes, collect skips (on_skip=skipped.append) and raise 409 with unloadable_node_refusal(skipped, BROWSER_WAY_THROUGH) before saving, or route them through get_dataset_state_for_mutation.

#### `ui/routes/table.py:519` — Primitive-list rows keep stale indices after a delete, so edits hit the wrong item

**Status: fixed in 0.34.0.**

delete_primitive_list_item pops the item at idx and the UI removes only that row (hx-swap="delete" in partials/inline_table.html line 111); the remaining rows keep their original {row._idx} in their hx-post/hx-delete URLs and nothing re-renders the table (entityChanged only reloads the tree sections in dataset.html, not the open editor). With list [a,b,c]: delete row 0, then editing the row for b (idx 1) runs `if idx < len(current_list): current_list[idx] = new_value` and overwrites c; editing the row for c (idx 2) fails the bound check and the edit is silently dropped while still returning 200 with HX-Trigger entityChanged. Additionally idx is an unconstrained int path param: idx=-1 passes `idx < len(current_list)` and edits/pops the last element, and idx <= -len raises IndexError -> unhandled 500 (update_primitive_list_item line 517-519 and delete_primitive_list_item lines 566-568).

*Verifier:* Confirmed from code. (1) table.py lines 517-520 and 566-569: bound-check-then-mutate by positional idx; out-of-range edits are silently dropped but still saved and answered 200 with HX-Trigger entityChanged. (2) inline_table.html lines 63-65 and 109-111: primitive-list rows embed the render-time list position (row._idx, assigned via enumerate in helpers/tables.py lines 142-144) in their hx-post/hx-delete URLs; delete uses hx-swap="delete" removing only that tr, edits use hx-swap="none", so surviving rows keep stale indices. (3) entityChanged only reloads #root-buttons and #entity-tree (dataset.html lines 31-34, 141-144) and hub.js only broadcasts to other tabs; no code re-renders the open editor or re-indexes rows. Therefore delete row 0 of [a,b,c] then edit the row showing b (idx 1) overwrites c, and edit the row showing c (idx 2) is silently discarded while reporting success — silent data corruption in the normal UI flow. The negative-idx claim also holds: idx is an unconstrained int path param, -1 passes idx < len and mutates the last element, idx <= -(len+1) raises an unhandled IndexError (500). Severity high is warranted.

**Fix:** After a primitive-list mutation, re-render the whole inline table partial (hx-target the table container) instead of swapping single rows, or address rows by stable ids rather than list position. Constrain idx with Path(ge=0) and return 404/409 when idx is out of range instead of silently saving.

#### `ui/spec_builder/routes/draft_routes.py:235` — Publishing a draft orphans datasets built on it

**Status: fixed in 0.34.0.**

publish_draft ends with `await session.delete(draft)`. Dataset.spec_draft_id is ForeignKey(ondelete="SET NULL"), so any dataset created from the draft loses its specification; on next load ensure_dataset_facade falls through to the built-in-profile branch, fails to resolve the user-spec profile name, and editing is disabled ('no specification is recorded for it'). delete_draft refuses deletion in exactly this situation via delete_draft_row's dependents check, but publish performs the same deletion with no check and nothing rebinds the datasets to the new Spec row (dataset.spec_id stays NULL).

*Verifier:* Confirmed from the code. publish_draft (draft_routes.py:235) unconditionally deletes the draft after creating the Spec row; Dataset.spec_draft_id is ForeignKey(ondelete="SET NULL") (models/datasets.py:53-56); the only writes to Dataset.spec_draft_id/spec_id occur at dataset creation (crud.py:575-624, spec_id stays None for draft-built datasets), so nothing rebinds dependents to the new Spec. ensure_dataset_facade (dataset_state.py:108-170) resolves the schema only via spec_draft_id, then spec_id, then the built-in-profile constructor; with both ids NULL a user-defined profile name fails to load and a dataset with stored entities raises DatasetDataLoadError ("Editing is disabled to protect the stored data"). entity_service.py mirrors this and also never resolves published specs by name, so no path rescues the dataset. delete_draft (access.py:480-510) refuses deletion in exactly this dependent-datasets situation, documenting the hazard the publish path ignores — an oversight, not a design choice. Severity high stands: a routine action (publishing) renders dependent datasets uneditable with no UI recovery.

**Fix:** On publish, either rebind dependent datasets (set spec_id to the new Spec and clear spec_draft_id) in the same transaction, or refuse publish while datasets depend on the draft, mirroring delete_draft.

#### `ui/spec_builder/routes/draft_routes.py:440` — update_profile_metadata saves an unvalidated version, bricking the draft

**Status: fixed in 0.34.0.**

`ctx.spec.version = version.strip() or "0.1"` assigns the raw form value. ProfileSpec's `_version_must_be_major_minor` validator only runs on construction/model_validate; the model has no `validate_assignment` (model_config is `ConfigDict(extra="forbid")` only), so an invalid value like `v1.0` or `draft` is accepted and persisted by `save_state_to_draft` via `model_dump`. Verified at runtime: assignment succeeds, and `ProfileSpec.model_validate` on the dumped dict then raises ValidationError. Every subsequent `load_state_for_draft` -> `dict_to_spec` raises `SpecVersionError`, and `handle_spec_version_error` tells the user to 'Correct it under Profile Settings in the draft editor' — but the draft editor itself goes through `load_state_for_draft` and can never open again. The remedy in the error page is unreachable for drafts corrupted through this route; versioning.py's whole 'way back' design assumes bad versions only come from rows that predate the rule, yet this route creates new ones.

*Verifier:* Confirmed end-to-end. draft_routes.py:440 assigns the raw form value to ctx.spec.version; ProfileSpec's model_config is ConfigDict(extra="forbid") with no validate_assignment, so the _version_must_be_major_minor field_validator does not run on assignment (reproduced at runtime in the project's venv: assigning "draft" succeeds). ctx.save -> save_state_to_draft persists spec.model_dump into draft.spec_data (and draft.version) without validation. Every subsequent load_state_for_draft -> SpecBuilderState.from_dict -> dict_to_spec raises SpecVersionError (reproduced), and handle_spec_version_error's remedy ("Correct it under Profile Settings in the draft editor") is unreachable because the editor itself loads via load_state_for_draft. Only nuance: save_state_to_draft re-caches the state per-worker, so the same worker keeps serving the editor from the in-memory cache until a restart/eviction or the request hits the other worker (production runs two) — the brick is inevitable but not always instantaneous. High severity stands: plausible user input (e.g. "v1.0", "1.0.0") permanently corrupts the stored draft with no UI recovery.

**Fix:** Validate with `check_profile_version(version)` (already imported in the sibling versioning module) before assigning, and return the metadata form with the problem text as `error` instead of saving. Add a red test posting `version="draft"` and asserting the draft still loads.

### Medium (30)

#### `alembic/versions/0053220bed75_add_spec_draft_id_to_project.py:33` — downgrade() calls op.drop_constraint(None, ...), which cannot execute

`op.drop_constraint(None, "projects", type_="foreignkey")` builds an unnamed constraint; SQLAlchemy raises CompileError ("it has no name") when emitting DROP CONSTRAINT, so this downgrade fails at runtime. The auto-generated "please adjust" stub was never adjusted, and migration tests only run upgrades (tests/test_migrations_match_models.py, tests/test_spec_version_migration.py), so nothing catches it.

*Verifier:* Verified in code and empirically. alembic/versions/0053220bed75_add_spec_draft_id_to_project.py line 33 contains op.drop_constraint(None, "projects", type_="foreignkey"). Running exactly that op through the project's installed Alembic with the PostgreSQL dialect raises CompileError: "Can't emit DROP CONSTRAINT for constraint ForeignKeyConstraint(...); it has no name". The repo has no SQLAlchemy naming_convention configured (the only mechanism that could name the generic constraint), so the failure is unconditional at runtime. No test in tests/ mentions or runs downgrades, confirming nothing catches it. The suggested fix matches existing repo convention (named FKs such as fk_datasets_spec_id in 260728_dataset_spec_link.py, dropped by name in downgrade). Severity medium is appropriate: the migration fails deterministically when invoked, but only on the rarely-exercised downgrade path, and Postgres transactional DDL means it aborts cleanly rather than corrupting the schema. Note the same defect also exists in d5b1a0f1f999_add_multi_spec_and_workspace_team_.py lines 131-132.

**Fix:** Name the FK in upgrade() (e.g. "fk_projects_spec_draft_id", as later migrations like 260728_dataset_spec_link do) and drop it by that name in downgrade(); or drop by the Postgres default name "projects_spec_draft_id_fkey".

#### `alembic/versions/d5b1a0f1f999_add_multi_spec_and_workspace_team_.py:131` — downgrade() is unrunnable: drop_constraint(None) twice and NOT NULL add without backfill

Lines 131-132 call `op.drop_constraint(None, "spec_drafts", type_="foreignkey")` twice — SQLAlchemy cannot emit DROP CONSTRAINT for an unnamed constraint, so this raises CompileError. Additionally line 128 re-adds `tenant_id` with `nullable=False` and no default or backfill, which fails on any populated spec_drafts table (contrast with this file's own upgrade(), which adds workspace_id nullable, backfills, then tightens). The "commands auto generated by Alembic - please adjust" markers were left in without the adjustment being made.

*Verifier:* Verified directly in /Users/sdrwacker/workspace/metaseed-project/metaseed-hub/alembic/versions/d5b1a0f1f999_add_multi_spec_and_workspace_team_.py. (1) Lines 131-132 call op.drop_constraint(None, "spec_drafts", type_="foreignkey") twice, matching the two unnamed FKs created in upgrade() at lines 116-121 via create_foreign_key(None, ...). SQLAlchemy cannot compile DROP CONSTRAINT for a nameless constraint and raises CompileError ("Can't emit DROP CONSTRAINT for constraint ...; it has no name"), so downgrade() fails on any database. (2) Lines 128-130 re-add tenant_id with nullable=False and no default or backfill anywhere in downgrade(); on a populated spec_drafts table Postgres rejects this with "column contains null values". The file's own upgrade() demonstrates the correct add-nullable/backfill/tighten pattern for workspace_id (lines 83-105), confirming the omission. The "please adjust" autogenerate markers remain at lines 127/159. Severity medium is correct: only the rollback path is broken; upgrade() and production forward migration are unaffected.

**Fix:** Name the two FKs created at lines 116-121 in upgrade() and drop them by name; in downgrade, add tenant_id nullable, backfill from the draft's workspace's tenant, then alter to NOT NULL.

#### `api/datasets.py:14` — API layer imports tenancy helpers from the UI layer

**Status: fixed in 0.33.1.**

`from metaseed_hub.ui.dependencies import get_tenant_for_user, verify_tenant_access` makes the REST API depend on the UI package, and main.py's websocket route similarly imports `get_dataset_for_user` from ui.dependencies. Tenant resolution (`tenant_slug_for`, `get_tenant_for_user`, `verify_tenant_access`, `get_dataset_for_user`) is shared authorization logic, not a UI concern; ui.dependencies also carries cookie/CSRF/HTMX machinery the API must never touch. The project rule is that boundaries are enforced with gate tests, and an api->ui import direction is exactly the kind of edge a gate test would forbid.

*Verifier:* Confirmed from the code: api/datasets.py:14 imports get_tenant_for_user and verify_tenant_access from metaseed_hub.ui.dependencies, and main.py:168 imports get_dataset_for_user from the same UI module for the websocket route. ui/dependencies.py is genuinely a UI module (ACCESS_TOKEN_COOKIE cookie name, validate_csrf_token, RedirectResponse, HTMX-aware AuthRequiredError), while the tenancy helpers it defines (tenant_slug_for:37, get_tenant_for_user:146, verify_tenant_access:216, get_dataset_for_user:254) are framework-neutral session/user logic — shared authorization, not a UI concern. No gate test forbids api->ui imports, even though the repo already uses boundary gate tests elsewhere (tests/test_no_forked_templates.py) and the project rules require enforced boundaries. Not a runtime bug, but a real layering violation; medium design severity is appropriate.

**Fix:** Move the tenancy/access helpers into a neutral module (e.g. metaseed_hub/tenancy.py or metaseed_hub/access.py) imported by both ui and api, and add an import-scanning gate test that metaseed_hub.api does not import metaseed_hub.ui.

#### `api/datasets.py:19` — REST API ignores DatasetMember sharing that the UI and websocket honor

**Status: fixed in 0.33.1.**

`_get_owned_dataset` scopes get/patch/delete strictly to `Dataset.tenant_id == tenant.id`. The UI path (`get_dataset_for_user` in ui/dependencies.py) and the websocket room authorization in main.py additionally grant access via `DatasetMember` rows. A user who was invited to a dataset can open it in the browser and join its collaboration room, but the same user's personal-access-token API calls get 404 for it, and `list_datasets` never shows it. If this restriction is deliberate (PATs act only on owned data), nothing in the docstrings or module docs says so; as written it reads as a divergence between two access models for the same resource.

*Verifier:* Confirmed from the code. api/datasets.py:19-55 (_get_owned_dataset) scopes get/patch/delete to Dataset.tenant_id == tenant.id with no DatasetMember check, and list_datasets (lines 88-114) uses verify_tenant_access which rejects any tenant but the caller's own, so shared datasets are invisible to the REST API. Meanwhile ui/dependencies.py:254-305 (get_dataset_for_user) grants access via DatasetMember rows and is used by all UI dataset routes, and main.py:168-185 authorizes websocket rooms through the same helper with a comment claiming it mirrors "the HTTP routes". No test pins the owner-only REST decision (test_api_datasets.py has no sharing test; test_cross_account_sharing.py covers only email provisioning), and no endpoint or module docstring documents it. The MCP layer is also tenant-owner-only (mcp/__init__.py:110, 527, 576), so the split may be a deliberate PAT-surfaces-are-owner-only policy, but nothing documents or tests that. Minor imprecision: _get_owned_dataset's docstring does say "owned by the caller's tenant", so the restriction is stated at the helper level, though not the deliberateness of excluding sharing. Severity medium is right: invited collaborators can edit a dataset in the browser but get 404 via personal-access-token API calls — a real functional divergence, restrictive rather than a security hole.

**Fix:** Either route the API through the shared `get_dataset_for_user` helper (mapping 403 to 404 if non-disclosure is wanted), or document explicitly that the REST API is owner-only and add a test pinning that decision.

#### `auth/__init__.py:217` — Token verification accepts symmetric algorithms advertised by the IdP

**Status: fixed in 0.33.1.**

verify_token passes `supported_algs = oidc_config.get("id_token_signing_alg_values_supported", [...])` straight into `jwt.decode(token, rsa_key, algorithms=supported_algs, ...)`. Keycloak's discovery document lists HS256/HS384/HS512 in that field, so the allowed-algorithm list at runtime includes symmetric HMAC algorithms while the key supplied is an RSA/EC JWK. The classic key-confusion attack (sign an HS256 token using the public key bytes as the HMAC secret) is currently blocked only because python-jose's HMACKey rejects a dict-shaped key -- an implementation detail of the dependency, not a property of this code. Additionally, `id_token_signing_alg_values_supported` describes ID tokens, and this code verifies access tokens; the hardcoded fallback list (asymmetric only) is safer than the discovery-derived list actually used.

*Verifier:* Confirmed from the code: auth/__init__.py lines 217-227 pass the discovery document's id_token_signing_alg_values_supported (which Keycloak populates with HS256/HS384/HS512) directly as the allowed-algorithm list to jwt.decode while supplying an RSA/EC public JWK dict. Verified in the installed python-jose 3.5.0 that a forged alg=HS256 header passes the allowlist check in jws._verify_signature (HS256 is in the list) and is stopped only by HMACKey._process_jwk rejecting kty != "oct" — the exact dependency implementation detail the finding cites, with the exception silently swallowed in _sig_matches_keys. pyproject.toml pins python-jose>=3.3.0 with no upper bound, so this safety property depends on unversioned third-party behavior. The field-semantics point (ID-token alg list used to verify access tokens; hardcoded asymmetric-only fallback is stricter than the runtime list) is also accurate. Medium severity is correct: real hardening defect in auth code, not currently exploitable with the resolved dependency.

**Fix:** Filter the discovery list to asymmetric algorithms (or intersect it with the hardcoded RS*/ES*/PS* set), or pass `algorithms=[rsa_key.get("alg")] if present`. Never allow HS* when verifying against a public JWK.

#### `features.py` — FeatureGrant has readers but no write path anywhere

enabled_features/has_feature read FeatureGrant rows, and require_feature in ui/dependencies.py guards routes on them, but grepping the whole repo (src, api, mcp, ui, scripts, ansible, alembic) finds nothing that creates, edits, or deletes a FeatureGrant — no admin UI, no API route, no CLI. Enabling a feature for a group requires hand-editing the database. This is exactly the defect sharing.py's own module docstring describes for the old SpecMember table ("a members table and nothing that wrote to it, so handing one over meant editing the database by hand"), and it violates the project rules that a capability not reachable from the UI is unfinished work and that code never reached is a defect.

*Verifier:* Confirmed by direct inspection. The only construction of FeatureGrant in the entire repo is tests/test_features.py:30; src/metaseed_hub/features.py (lines 46-48) and require_feature in src/metaseed_hub/ui/dependencies.py (line 418, used by ui/routes/seek.py:49) only read it. The alembic migration 260808_feature_grants.py creates the table with no seed data, and grepping scripts/, ansible/, Makefile, and docs/ finds no admin UI, API route, CLI, playbook task, or operator documentation for creating grants. CHANGELOG.md line 297 states "No grants ship, so every gated feature starts off", so the SEEK plugin and DCAT field marker are unreachable in any deployment without hand-editing the database — exactly the defect sharing.py's own docstring (line 7) condemns for the old SpecMember table. The one refutation path (an intentional, documented operator mechanism) does not exist: docs/administration.md has zero mentions of features or grants. Severity medium is correct: the read path works, so this is an unfinished-capability/design gap, not a runtime bug, but it strands two shipped gated features.

**Fix:** Add a grant-management surface (admin dashboard section listing SRAM group URNs with add/remove grant actions) in the same change stream, or document explicitly that grants are operator-managed via a supported mechanism and provide that mechanism (e.g. a make/CLI target), with tests.

#### `mcp/__init__.py:398` — _published_spec resolves cross-tenant name collisions nondeterministically

Published specs are looked up hub-wide (deliberately not tenant-scoped), but the uniqueness constraint on specs is `uq_specs_tenant_name_version` (tenant, name, version) — so two tenants can each publish a spec with the same name and version. `_published_spec` ends with `return result.scalars().first()` on a query with no ORDER BY: which tenant's spec wins is decided by row order and can differ between calls. create_dataset then binds `spec_id = published.id` to an arbitrary one of them, and `_profile_spec` (used by validate_dataset via get_profile_schema/get_profile_relationships for name-based resolution) may resolve a different one on another call. list_profiles similarly reports both as indistinguishable entries `{"name": s.name, "versions": [s.version]}`.

*Verifier:* Confirmed from the code. The unique index uq_specs_tenant_name_version (models/specs.py:56-63) is scoped to (tenant_id, name, version), and the publish flow's only collision check (draft_routes.py:204, latest_published_spec) is tenant-scoped, so two tenants can each publish a spec with the same name and version. _published_spec (mcp/__init__.py:390-398) queries hub-wide with no tenant filter and no ORDER BY, returning .scalars().first() — an arbitrary, potentially varying row when both exist. create_dataset (line 602) binds spec_id from that arbitrary pick while get_profile_schema resolves name-based via _profile_spec, so an agent can be shown one tenant's schema and get a dataset bound to the other tenant's spec; list_profiles (lines 703-706) reports both as indistinguishable entries. One detail in the finding is inaccurate: validate_dataset resolves via the dataset's stored spec_id (ensure_dataset_facade), so validation of an existing dataset is stable — the nondeterminism lives in the name-based tools, not validate_dataset. Severity medium is correct: it needs a cross-tenant (name, version) collision to trigger and causes silent wrong-schema resolution, not data loss or a security breach.

**Fix:** Either enforce hub-wide uniqueness of published (name, version) at publish time, or make the lookup deterministic and the ambiguity visible: detect multiple matches and refuse with a message naming the collision, and have list_profiles disambiguate (e.g. include the publishing tenant or spec id).

#### `mcp/__init__.py:639` — save_dataset bypasses the canonical write-path invariants (no spec-hash stamp, no tree envelope)

**Status: fixed in 0.34.0.**

The `_editing` context writes `stamp_spec_hash(make_json_serializable(client.serialize(format="tree")), ...)` and its comment states the tree format is the hub's canonical storage ('readers such as the version diff view consume data["tree"]. A flat write here would make those readers see an empty dataset') and that stamping makes 'an agent's write and a person's ... indistinguishable to the drift check'. But the save_dataset tool stores the caller's payload verbatim: `dataset.data = data` with no stamp_spec_hash and no shape check. Consequences: (1) a full replace via save_dataset drops the provenance stamp, so spec_drift_message silently reports nothing ('unknown provenance') for every dataset last written this way; (2) an agent can store a flat `{"entities": [...]}` payload that validates fine through the facade but renders empty in tree-consuming web readers. tests/test_dataset_spec_hash.py::TestStamping is documented as 'Every write path records the hash; nothing else has to remember to', yet it only covers the entity-edit path — this write path does not record it.

*Verifier:* Confirmed from code. mcp/__init__.py:639 stores the caller's payload verbatim (dataset.data = data) with no stamp_spec_hash and no shape normalization, while the only stamping write paths are _editing (mcp/__init__.py:318) and the web save (ui/helpers/dataset_state.py:224). spec_hash.py:142-144 shows spec_drift_message returns None for unstamped envelopes, so datasets last written via save_dataset get no drift reporting. ui/routes/dataset/versions.py:49-50,96-97 read data.get("tree", []) directly, so a flat {"entities": [...]} payload stored by save_dataset renders as empty in the version diff view even though it validates through the facade (which handles legacy flat format). tests/test_dataset_spec_hash.py:74-75 claims "Every write path records the hash" but only covers save_dataset_state, not this tool. Severity medium is correct: no data loss (pre-write snapshot exists, main view loads via the facade), but a documented-and-tested invariant is violated by one write path and a real UI reader degrades.

**Fix:** In save_dataset, stamp the payload with `stamp_spec_hash(data, await dataset_spec_hash(session, dataset))` before storing, and either normalize the payload through the facade (load + serialize(format="tree")) or at least reject payloads that carry neither 'tree' nor a loadable legacy shape. Extend the stamping test to cover save_dataset.

#### `mcp/_ontology_tools.py:121` — OLS outage reported as 'term does not exist'; the outage catch is unreachable

**Status: fixed in 0.34.0.**

get_ontology_term wraps get_term_source().get_term(term_id) in `except OntologyServiceError`, but get_term_source() returns metaseed's TermRouter, whose get_term_sync catches every per-source exception (`except Exception: ... continue`, metaseed/src/metaseed/services/terms.py lines 156-172) and returns None. An OLS outage therefore never raises here; it surfaces as `term is None`, and the tool answers `raise ValueError(f"No ontology term {term_id!r}...")` — affirmatively asserting the term does not exist while the service was merely down. This violates the project rule that an outside-service outage must report 'not checked', never a failure. The `except OntologyServiceError` branch is effectively dead code that suggests the outage case is handled when it is not. search_ontology and suggest_ontology_term have the milder form of the same masking (outage indistinguishable from zero results).

*Verifier:* Confirmed from the code. TermRouter.get_term_sync (metaseed/src/metaseed/services/terms.py:155-172) catches every per-source exception with `except Exception: ... continue` and returns None; the async get_term (lines 359-368) just runs it in a thread, so OntologyServiceError raised by OntologyService.get_term_sync on OLS outage (ontology.py:640,645) never reaches the hub tool. The `except OntologyServiceError` at _ontology_tools.py:121 is therefore unreachable, and an outage surfaces as term is None, making the tool raise "No ontology term {term_id!r}..." — asserting nonexistence when the service was merely down. The source file is byte-identical to the copy in the hub's .venv, so deployed behavior matches. This also contradicts the router module's own documented contract that an unanswerable lookup is reported as "not checked", and the project rule that an outside-service outage must never be reported as a failure. search_ontology/suggest_ontology_term share the milder masking via search_sync's identical continue-on-exception (terms.py:326-333). Severity medium is appropriate: an MCP client is affirmatively told a valid term does not exist during an outage, which can drive wrong downstream edits, but no data is corrupted directly.

**Fix:** Have the router (or an adapter-level capability) distinguish 'source unavailable' from 'term absent' — e.g. get_term returning a sentinel or the router exposing which sources failed — and make get_ontology_term report 'the ontology service could not be reached; not checked' in that case instead of claiming nonexistence. Remove or repurpose the unreachable OntologyServiceError catch.

#### `mcp/_rule_tools.py:81` — applies_to narrowed to str; multi-entity rules are unauthorable over MCP

spec_add_rule and spec_update_rule declare `applies_to: str | None = None`, but metaseed's ValidationRuleSpec declares `applies_to: list[str] | str = "all"` (metaseed/src/metaseed/specs/schema.py line 436). A rule that applies to several named entity types cannot be expressed through these tools — exactly the 'silently unauthorable over MCP' case `_rule_attributes` and the parity gate exist to prevent. tests/test_mcp_rule_parity.py checks only parameter *names* against RULE_ATTRIBUTE_NAMES, so this type narrowing is invisible to the gate.

*Verifier:* Confirmed from code. Both MCP tools declare applies_to: str | None (_rule_tools.py lines 81, 148) while ValidationRuleSpec declares applies_to: list[str] | str = "all" (metaseed/src/metaseed/specs/schema.py line 436, documented "List of entity names or 'all'"). The list form is genuinely supported elsewhere: the hub web UI parses comma-separated input into a list (ui/spec_builder/forms.py get_applies_to, lines 112-117), and both the builder (builder.py 799-804) and hub entity routes (entity_routes.py 200-201, 258-262) handle list-valued applies_to on rename/delete. FastMCP validates arguments against a pydantic model built from the signature, so a JSON array is rejected; the tool body passes the value through _rule_attributes(locals()) to builder.add_rule with no splitting, so a comma-joined string would be one unknown entity name flagged by validation, not a workaround. The parity gate (tests/test_mcp_rule_parity.py lines 26-27) compares only parameter names against RULE_ATTRIBUTE_NAMES, so the type narrowing is invisible to it — exactly the failure mode the module's own docstring says the design exists to prevent. Severity medium is correct: real capability gap over MCP, but no data corruption and the web UI covers it.

**Fix:** Widen the parameter to `applies_to: list[str] | str | None = None` in both tools (and document the list form), or add a parity check on parameter annotations against the model's field types.

#### `tokens.py:106` — authenticate_token authenticates soft-deleted users

**Status: fixed in 0.33.1.**

authenticate_token ends with `return await session.get(User, token.user_id)` with no check of User.deleted_at. The User model carries SoftDeleteMixin, and every other user lookup in the hub treats a soft-deleted user as nonexistent (sharing.account_for_email filters `User.deleted_at.is_(None)`, admin routes filter it everywhere). A soft-deleted user's personal access token therefore still authenticates and reaches their tenant's data via the MCP endpoint and the API bearer path (auth/__init__.py:305, mcp/__init__.py:96), neither of which re-checks deleted_at. No current admin route soft-deletes users, so this is latent, but the mixin plus soft_delete()/restore() exist and the inconsistency will become a live credential-revocation hole the moment user removal ships.

*Verifier:* Confirmed directly from code. tokens.py:106 returns `await session.get(User, token.user_id)` with no deleted_at check; User has SoftDeleteMixin (models/identity.py:51) with soft_delete()/restore(); every other user lookup filters User.deleted_at.is_(None) (sharing.py:224, ui/routes/admin.py:124/145/218/294/308/325, ui/spec_builder/access.py:668); and neither caller re-checks — auth/__init__.py:305 wraps the user into a TokenUser for API access and mcp/__init__.py:96 yields the user to every MCP tool. The finding's latency claim is also accurate: soft_delete() is only invoked for Dataset and Spec (admin REMOVABLE covers only those kinds), so no shipped route soft-deletes a User. It is nonetheless a real invariant violation in the authentication path: a user soft-deleted via direct DB update (or any future removal feature) would still authenticate via personal access token while being nonexistent everywhere else. Medium severity is appropriate for a latent credential-revocation gap.

**Fix:** In authenticate_token, return None when the loaded user is soft-deleted (e.g. `user = await session.get(User, token.user_id); return None if user is None or user.deleted_at is not None else user`), and add a red-first test that a soft-deleted user's token no longer authenticates.

#### `ui/explore_routes.py:94` — Explore catalog offers shared drafts that load_profile_spec then refuses

_build_explore_catalog includes drafts shared with the user via SpecDraftMember (lines 239-249) so they appear as selectable profiles, but load_profile_spec's draft branch scopes strictly to SpecDraft.tenant_id == tenant_id (the caller's own tenant). Comparing a shared draft therefore always resolves to None and the request fails with 'No valid profiles found' / an empty graph, even though the same user can open that draft in the spec builder (get_draft_role grants member access).

*Verifier:* Confirmed from code. _build_explore_catalog (explore_routes.py:239-259) offers drafts shared via SpecDraftMember as selectable draft:{id} profiles; since tenants are per-user (tenant_slug_for hashes keycloak_id), these are always foreign-tenant drafts. load_profile_spec's draft branch (lines 94-109) requires SpecDraft.tenant_id == caller's tenant.id (passed as tenant.id at lines 358/416), so shared drafts always resolve to None. get_draft_role (spec_builder/access.py:196-205) grants members access regardless of tenant, confirming the builder/explorer inconsistency. Minor correction to the symptom: profile_tuples is populated before the load attempt (line 166), so the 400 'No valid profiles found' branch is not hit; instead HubSpecLoader falls back to the built-in loader with the 'draft:xxx' key, which raises and returns a 500 (lines 389/427). Same user-visible outcome: selecting a shared draft in the explorer always fails. Severity medium is correct.

**Fix:** In load_profile_spec, accept a draft when the caller's tenant owns it OR the caller is a SpecDraftMember (pass user_id alongside tenant_id), matching get_draft_role.

#### `ui/helpers/entity_import.py:88` — Unmatched _parent references are silently flattened to root during import

add_entities_in_order attaches each row with `parent_id=node_by_identifier.get(parent_key)`. When parent_key is non-empty but not (yet) in the map, the node is silently created at root with no entry in `errors` — the import reports full success while the hierarchy is flattened. This also makes the result order-dependent: processing is by type in `[root_entity] + facade.entities` declaration order, not topological order, so a child whose parent type is declared later in the spec can never find its parent even when the workbook data is correct.

*Verifier:* Confirmed at entity_import.py line 88: node_by_identifier.get(parent_key) returns None for a non-empty unmatched _parent, the node is created at root (store.add_entity accepts parent_id=None without error), imported is incremented, and errors receives nothing — the editor.py caller then reports "Successfully imported N entities". Order dependence is also real: entity_order (line 64) is [root] + facade.entities, which is list(self._entities.keys()) in YAML/spec declaration order; loader.py:415-416 documents it as only "typically hierarchical" and no topological sort exists, so a parent type declared after a child type can never be found. One unstated mitigation: store.add_entity falls back to _resolve_parent via reference fields (store.py:127-130), so rows carrying a populated reference field (e.g. investigation_id) whose parent already exists in the index still attach correctly — but this fails precisely in the out-of-order case and whenever the reference field is empty or _parent is mistyped, and the import still reports success. Tests cover only the happy path. Medium severity is correct: silent data-structure corruption on import, partially mitigated.

**Fix:** Record an error (or at least a warning in the returned list) when a non-empty _parent does not resolve, and process types in hierarchy (topological) order or do a second pass to re-attach rows whose parents were created later.

#### `ui/routes/auth.py:347` — Personal access token secret is round-tripped through a URL query parameter

**Status: fixed in 0.33.1.**

auth_create_token redirects with the freshly minted secret in the URL: `return RedirectResponse(url=f"/hub/auth/profile?token={secret}", status_code=303)`, and auth_profile reads it back via `request.query_params.get("token")`. The comment says it is "not stored anywhere", but a query string is: it lands in server/proxy access logs, in the browser history, and can leak via the Referer header from the profile page. For a bearer credential that authenticates the MCP endpoint this undermines the care taken elsewhere (key encrypted at rest in seek.py, token shown only once).

*Verifier:* Verified in metaseed-hub (actual path /Users/sdrwacker/workspace/metaseed-project/metaseed-hub/src/metaseed_hub/ui/routes/auth.py, not the metaseed path given). Line 347 redirects with the plaintext secret in the query string; line 317 reads it back via request.query_params.get("token") for one-time display. tokens.py confirms the secret is a long-lived bearer credential stored only as a SHA-256 hash, and authenticate_token() is used by mcp/__init__.py:96 and auth/__init__.py:305 to authenticate MCP/API requests. The plaintext secret therefore lands in server/proxy access logs (query strings logged by default) and browser history, contradicting the hash-only-at-rest design. The Referer-leak vector is weak under modern default referrer policy, but the log and history sinks alone confirm the finding. Medium severity is correct: real leak vectors, but exploitation needs log/history/shared-machine access.

**Fix:** Carry the secret server-side for the one redirect instead of in the URL: stash it in the signed session/one-time server-side slot keyed by user (or render the profile page directly from the POST response), so the secret never appears in a URL.

#### `ui/routes/table.py:137` — _get_default_values fabricates placeholder data and persists it with validation skipped

For a new entity row, required string fields get invented values like `f"New {fname.replace('_', ' ').title()}"`, identifiers get `str(uuid.uuid4())[:8]`, numbers get 0/0.0, and the instance is saved via `model_construct` with `skip_validation=True`. This directly contradicts the project's stated data principle (leave unknown fields empty so validation reports them; an empty required field is a smaller problem than a wrong one). A user who never edits a cell ships a dataset whose required fields hold fabricated strings that satisfy validation, silently masking missing data.

*Verifier:* Confirmed in src/metaseed_hub/ui/routes/table.py: _get_default_values (lines 137-153) invents values for required fields (uuid[:8] for identifiers, "New <Field Name>" for strings, 0/0.0/False for numerics/booleans), and _add_entity_list_row (lines 353-369) persists them via model_construct + add_node(skip_validation=True) + save_dataset_state, so the fabricated data reaches the database. The contradicted principle is stated verbatim in the same codebase (src/metaseed_hub/mcp/__init__.py lines 240, 480, 732: "empty required field is a smaller problem than inventing a value"). The suggested fix is viable: the save path already skips validation so blank fields persist, and the cell renderer already shows a "Click to edit" placeholder for empty values (lines 92, 216-219). Fabricated non-empty strings pass required-field validation, silently masking missing data. Medium severity is correct: a real data-integrity/design defect, but not a crash or security issue.

**Fix:** Create the row with empty values (the code already saves with skip_validation, so blanks persist fine) and let validation report the truly missing required fields; keep only the parent-reference auto-fill, which is derived rather than invented.

#### `ui/routes/table.py:158` — Hand-built row HTML forks partials/inline_table.html and has already drifted

_build_entity_row_html and _build_primitive_row_html rebuild in Python strings the exact row markup that partials/inline_table.html renders for the same tables. The fork has already diverged: template rows attach reference-field autocomplete (`lookup-input` class, data-lookup/data-lookup-field/data-lookup-url attributes, inline_table.html lines 68-98) which lookup_entities in this same file exists to serve, but a row returned by add_table_row has none of them - a freshly added row lacks the reference dropdown until the page is reloaded. Every future change to the table markup must now land twice, which is exactly the drift the project's anti-fork rule exists to prevent (applied here to hub-internal duplication of its own template).

*Verifier:* Confirmed by reading the code. _build_entity_row_html (src/metaseed_hub/ui/routes/table.py:158-239) and _build_primitive_row_html (:56-106) duplicate the row markup of partials/inline_table.html, and the drift the finding describes is verifiable: the template's entity-list rows (inline_table.html:85-101) attach lookup-input class and data-lookup/data-lookup-field/data-lookup-url attributes for reference fields (fed by _reference_fields in ui/helpers/tables.py:293-309), while the Python-built cell (table.py:218-223) emits only class="cell-input" and receives no reference-field data at all. lookup.js initializes autocomplete via querySelectorAll('.lookup-input'), so a row returned by add_table_row — swapped beforeend into the tbody by the + Add Row button (inline_table.html:23-28) — has no reference autocomplete. The HX-Trigger: entityChanged response header only re-renders the sidebar root-buttons and tree (dataset.html:33,143), not the editor table, so the degraded row persists until the entity form is reloaded. lookup_entities (table.py:745) is the route the missing attributes would call. Additional minor drift: template truncates values over 50 chars, Python builder does not. Severity medium is appropriate: real functional gap on newly added rows plus a maintenance fork, but no data corruption and a re-render restores the lookup.

**Fix:** Render the new row through the same Jinja partial (extract a row macro/partial from inline_table.html and render it via render_template for both the full table and the single-row responses), then delete _build_entity_row_html and _build_primitive_row_html.

#### `ui/routes/table.py:459` — helper.field_info() raises KeyError on caller-controlled field names; unhandled -> 500

metaseed's EntityHelper.field_info raises KeyError for an unknown field (facade/helper.py line 289). Four call sites pass caller-controlled names without catching it: update_table_cell line 459 (`info = helper.field_info(field_name)` for every non-underscore form key), add_table_row line 279 (try block catches only AttributeError), update_single_entity_field line 617, and delete_single_entity_field line 721. A stale form after a spec change, or a crafted request, produces an unhandled 500. Worst is delete_single_entity_field: the KeyError fires after the mutation has already been saved (line 717), so the state change commits and the client still gets a 500.

*Verifier:* Verified in code: metaseed's EntityHelper.field_info raises KeyError for unknown fields (facade/helper.py line 289), while unknown entity types raise AttributeError — so the routes' existing `except AttributeError` guards do not cover unknown field names. All four cited call sites in src/metaseed_hub/ui/routes/table.py pass caller-controlled field names (form keys at line 459; path parameters at 279, 617, 721) with no KeyError handling: line 279 sits in a try catching only AttributeError, lines 459/617 have no try at all, and line 721 runs after save_dataset_state (line 717), so the mutation persists before the 500 is returned — exactly as the finding claims. The hub app registers only four specific exception handlers (app.py 163-189) and no generic handler, so Starlette's ServerErrorMiddleware returns 500 for the unhandled KeyError. Severity medium is correct: triggering requires authenticated mutation access to the dataset, and the impact is unhandled 500s (plus one save-then-error sequence), not data corruption.

**Fix:** Catch KeyError alongside AttributeError at each site (or add a helper that returns None for unknown fields) and return the same 400-style HTMLResponse the routes already use for unknown entity types. In delete_single_entity_field, look up field_info before mutating.

#### `ui/services/entity_service.py:101` — EntityService duplicates the dataset load path with different semantics

EntityService.ensure_state, _load_client_for_draft_spec, _load_client_for_published_spec, and _load_tree_data reimplement ensure_dataset_facade (helpers/dataset_state.py), whose docstring calls itself 'the single load path'. Both resolve draft/published/builtin specs and unwrap the SpecBuilderState 'spec' envelope; fixes must land twice (the CLAUDE.md anti-fork rule, applied inside the hub). They also behave differently: EntityService loads strictly (self._client.load(data) with no on_skip), so a dataset with one unplaceable stored node cannot be opened at all through the entity routes, while ensure_dataset_facade loads it permissively and reports the skips. Two failure modes for the same dataset depending on which route touches it.

*Verifier:* Confirmed from code. EntityService (src/metaseed_hub/ui/services/entity_service.py lines 101-292) duplicates ensure_dataset_facade (src/metaseed_hub/ui/helpers/dataset_state.py lines 55-197): same draft/published/builtin spec resolution, same "spec" envelope unwrapping (lines 200-201, 245-246 vs _client_from_spec_data lines 36-37); the fork is even self-documented ("Mirrors ensure_dataset_facade", line 221) while ensure_dataset_facade's docstring claims to be "the single load path". The behavioral divergence is verified in the metaseed library: MetaseedClient.load (metaseed/src/metaseed/api/serialization.py) is strict without on_skip ("if on_skip is None: raise", lines 216-217; docstring: "one bad node makes the whole dataset unreadable"). EntityService calls self._client.load(data) with no on_skip (line 284) and converts any failure to DatasetDataLoadError, while ensure_dataset_facade always passes on_skip=report (line 185) and loads permissively. Both paths are live: all five entity CRUD routes (ui/routes/entity.py) use EntityService; editor/table/crud/seek routes and MCP use ensure_dataset_facade. A dataset with one unplaceable stored node therefore opens in the editor but fails all entity mutation routes — two failure modes for the same dataset depending on route. Medium severity is right: real user-visible inconsistency and a double-fix maintenance trap, but no data corruption on the strict path (which is arguably the safer behavior, a nuance the fix direction should weigh).

**Fix:** Have EntityService.ensure_state delegate to ensure_dataset_facade (with skip collection and the 409 refusal on mutation), deleting the duplicated spec-resolution and load code.

#### `ui/spec_builder/access.py:131` — can_edit_spec docstring contradicts its implementation

The docstring states 'Its author, and nobody else' and 'Sharing a published specification with another person is not possible today (``spec_members`` has no interface writing to it); when it returns, it belongs here.' The function body then checks `role_of(session, resource_for("spec"), spec.id, user_id) in EDIT_ROLES`, and the generic sharing routes (src/metaseed_hub/ui/routes/sharing.py, kind="spec") do write spec_members rows. Related stale comments in draft_routes.py repeat claims the code does not implement: unpublish_spec_endpoint says 'its publisher, and tenant admins and owners' and view_spec says 'only its author or a tenant admin can change it', but can_edit_spec has no tenant-admin branch. Wrong prose on an authorization function misleads the next security-relevant change.

*Verifier:* Confirmed from the code. (1) In /Users/sdrwacker/workspace/metaseed-project/metaseed-hub/src/metaseed_hub/ui/spec_builder/access.py, the can_edit_spec docstring (lines 136-143) states "Its author, and nobody else" and "Sharing a published specification with another person is not possible today (``spec_members`` has no interface writing to it); when it returns, it belongs here" — yet the function body at lines 158-161 imports EDIT_ROLES/resource_for/role_of from metaseed_hub.sharing and returns True for any user shared on the spec with owner or editor role. The inline comment at lines 156-157 ("Until published specifications could be shared at all, this was the author and nobody else") even acknowledges sharing now exists, so the docstring contradicts both the body and its own inline comment. (2) spec_members IS writable through an interface: src/metaseed_hub/sharing.py registers kind="spec" mapping to SpecMember (lines 131-137), and src/metaseed_hub/ui/routes/sharing.py exposes POST/PATCH/DELETE /{kind}/{resource_id}/members which call add_member/set_role/remove_member, writing SpecMember rows. That module's own header says published specs previously "had a members table and nothing that wrote to it" — past tense, fixed by that module. (3) The stale comments exist as described, in src/metaseed_hub/ui/spec_builder/routes/draft_routes.py (note: routes/ subdirectory, slightly different path than the finding states): line 294-295 in view_spec says "only its author or a tenant admin can change it", and lines 393-394 in unpublish_spec_endpoint say "its publisher, and tenant admins and owners" — but can_edit_spec has no tenant-admin branch at all; its docstring explicitly records that the team/admin branch was removed. Severity medium is defensible: no runtime defect, but the false prose sits directly on an authorization function and asserts capabilities (tenant-admin access) and absences (no sharing interface) that could mislead the next security-relevant change in either direction.

**Fix:** Rewrite the docstring to state the actual rule (author, or a user shared on the spec with an EDIT role) and correct the two comments in draft_routes.py that mention tenant admins.

#### `ui/spec_builder/access.py:272` — require_draft_owner has no production caller

Grep across src/ finds no caller of `require_draft_owner`; only tests/test_security.py exercises it directly. Its docstring says it is 'Used to gate membership management', but membership management now goes through the generic sharing routes (src/metaseed_hub/ui/routes/sharing.py backed by metaseed_hub/sharing.py); the former member_routes module is gone (only a stale routes/__pycache__/member_routes.cpython-313.pyc remains). Per the project rule, code written but never reached is a defect, and the tests currently certify a function nothing uses.

*Verifier:* Verified directly in the code. require_draft_owner is defined at src/metaseed_hub/ui/spec_builder/access.py:272 and a repo-wide grep finds no caller anywhere in src/ — the only references are tests/test_security.py:150-195 and prose in REVIEW.md. Every module importing from spec_builder.access (app.py, routes/_common.py, draft_routes.py, list_routes.py, comment_routes.py, mcp/_spec_tools.py) imports other names, never this one. The former caller member_routes.py no longer exists as source; only the stale spec_builder/routes/__pycache__/member_routes.cpython-313.pyc remains. Draft membership management now goes through src/metaseed_hub/ui/routes/sharing.py backed by src/metaseed_hub/sharing.py, which registers a "draft" SharedResource over SpecDraft/SpecDraftMember and whose docstring states the drafts' dedicated router was replaced. The function's docstring ("Used to gate membership management") is therefore stale, and its tests certify code nothing reaches — exactly the defect the project's dead-code rule describes. Medium severity is appropriate given the misleading docstring on a security-gating helper and the project's explicit rule treating unreached code as a defect.

**Fix:** Delete require_draft_owner and its tests, or wire it into whatever still needs an owner-gated draft load; also clear the stale member_routes pycache artifact.

#### `ui/spec_builder/routes/draft_routes.py:220` — Republishing identical content 500s on the unique index

**Status: fixed in 0.34.0.**

bump_refusal returns None when required_bump is 'none' ('republishing it cannot invalidate anything, so there is nothing to refuse'), so publishing a draft whose content and version equal an already-published spec proceeds to `session.add(spec)` and commit, violating the partial unique index uq_specs_tenant_name_version. The IntegrityError is unhandled — the user gets a 500 instead of a message, unlike create_new_spec in list_routes.py which catches IntegrityError and returns a 409 with an explanation.

*Verifier:* Confirmed from code. bump_refusal (versioning.py:172-176) returns None when required_bump is 'none' (identical content per compare.py:836-838), so publish_draft (draft_routes.py:204-236) proceeds to session.add(spec)/commit with the same (tenant_id, name, version) as the still-live published row. uq_specs_tenant_name_version (models/specs.py:56-63) is a unique partial index where deleted_at IS NULL, and latest_published_spec only returns rows with deleted_at NULL, so the insert violates it. No IntegrityError handling exists in draft_routes.py and no app-wide handler covers it (app.py registers only AuthRequiredError, DuplicateAccountEmailError, DraftConflictError, SpecVersionError), so the user gets a 500. Reachable from the UI: create_draft_from_spec (the edit-published-spec flow) copies name and version into a draft in the user's own tenant; publishing it unchanged triggers the collision. list_routes.py:176-184 shows the codebase's established pattern of catching IntegrityError and returning 409, which this path lacks. Severity medium is correct: user-facing 500 in a plausible flow, but the transaction rolls back atomically so no data is lost or corrupted.

**Fix:** Catch IntegrityError around the publish commit (rollback and render save_result.html with a 'this name and version is already published' error), or check for an existing (tenant, name, version) row before inserting.

#### `ui/spec_builder/routes/draft_routes.py:233` — publish_draft can 500 on the (tenant, name, version) unique index

**Status: fixed in 0.34.0.**

Spec has partial unique index `uq_specs_tenant_name_version` (deleted_at IS NULL). `bump_refusal` deliberately returns None when `required_bump` is "none" (identical content), so republishing an unchanged spec under an already-published version — or declaring a version equal to an older published release with content identical to the latest — reaches `session.add(spec)`/commit and raises an unhandled IntegrityError, surfacing as a 500. `create_new_spec` in list_routes.py catches the analogous IntegrityError for drafts; the publish path does not.

*Verifier:* Confirmed from code. models/specs.py:56-63 defines partial unique index uq_specs_tenant_name_version on (tenant_id, name, version) WHERE deleted_at IS NULL. versioning.py:172-176 has bump_refusal return None when required_bump is "none" (identical content per compare_specs), so the publish gate deliberately passes. draft_routes.py:220-236 then does session.add(spec)/commit with no prior duplicate check and no IntegrityError handling; ui/app.py registers no IntegrityError handler. Reachable UI flow: create_draft_from_spec forks a live published spec in the same tenant keeping name/version/content; publishing the fork unchanged collides with the still-live original on the index, raising an unhandled IntegrityError (500). list_routes.py:176-181 catches the analogous IntegrityError for drafts, confirming the asymmetry. Severity medium is right: commit rollback means no data loss (draft survives), but a legitimate user action yields a 500 instead of a friendly message.

**Fix:** Before inserting, check for a live Spec with the same (tenant_id, name, version) and return a save_result partial explaining the version is already published (or treat identical-content republish as a no-op); alternatively catch IntegrityError, rollback, and render the same friendly message.

#### `ui/spec_builder/routes/list_routes.py:228` — Spec YAML import reads the upload unbounded

import_spec does `content = await spec_file.read()` with no size cap. helpers/uploads.py exists precisely for this — its comment calls the bare read 'a memory-pressure DoS on a deployment without an edge cap' — and every dataset import route uses read_upload_capped. The spec-builder import is the one upload endpoint that skips it.

*Verifier:* Verified directly in the code. list_routes.py:228 does `content = await spec_file.read()` with no bound, then decodes and YAML-parses the full content in memory. helpers/uploads.py defines read_upload_capped with a 25 MiB cap and its comment explicitly names the bare read a memory-pressure DoS; both dataset import routes (dataset/editor.py:714, dataset/crud.py:204) use it, and a repo-wide grep shows the spec-builder import is the only upload endpoint with an uncapped read. Starlette's UploadFile.read() with no argument returns the entire upload, so the framework semantics match the claim. The route is authenticated (UserContextDep), same as the capped routes, so medium severity for a consistency/hardening gap is appropriate. The suggested fix matches the existing error-rendering pattern in the same try/except.

**Fix:** Use `content = await read_upload_capped(spec_file)` and render the 413 as the form error the other branches use.

#### `ui/spec_builder/routes/list_routes.py:267` — GET /clone/{profile}/{version} creates a draft (state-changing GET)

`clone_template` is registered with `@router.get` but calls `create_new_draft`, committing a new SpecDraft row. It is linked as a plain `<a href>` from spec_builder/start.html. A state-changing GET is exempt from the app-wide Origin-based CSRF guard (which covers unsafe methods only), so a cross-site `<img src="/hub/spec-builder/clone/x/1.0">` can create drafts in a signed-in user's account, and browser prefetching/crawling of the link creates drafts unintentionally. The sibling create paths (`POST /new`, `POST /import`) are correctly POSTs.

*Verifier:* Verified in code: list_routes.py line 266-267 registers clone_template with @router.get, and it calls create_new_draft (access.py lines 461-472), which session.add()s and commits a SpecDraft row — a state-changing GET. start.html:37 links it as a plain <a href>. The app-wide CSRF guard require_same_origin (security.py, wired app-wide in app.py:97) returns early for methods not in _UNSAFE_METHODS, so GET is never origin-checked. One detail of the finding is wrong: the session cookie is SameSite=Lax (auth.py), so the cited <img src> vector would not carry credentials; but Lax cookies ARE sent on top-level cross-site GET navigations, so a cross-site link click or window.location still creates drafts in a signed-in user's account, and browser link prefetching can create drafts unintentionally. The core defect stands; medium severity is correct (nuisance draft creation, no data loss).

**Fix:** Change the route to POST and render the start-page links as small forms or hx-post buttons; keep the redirect-to-editor response.

#### `websocket/__init__.py:231` — Presence lists are per-instance despite Redis pub/sub scaling claim

The class docstring says the manager uses "Redis pub/sub for scaling", and message broadcasts do fan out across instances. But `Room.connections` is process-local state, and join/leave broadcasts embed `"presence": room.get_presence()` -- only this instance's connections. With two app instances, a user joining on instance A publishes a presence list that omits everyone connected on instance B, so every client's presence view is wrong the moment a second instance exists. Message delivery scales; presence does not.

*Verifier:* Confirmed in src/metaseed_hub/websocket/__init__.py: Room.connections (line 34) and manager._rooms (line 68) are in-process dicts; get_presence() (lines 44-53) reads only local connections. join_room/leave_room embed "presence": room.get_presence() (lines 231, 257) in messages that broadcast_to_room publishes via Redis (lines 282-290) and every instance's listener delivers to its clients (_dispatch_local). Thus with two instances, a join on instance A pushes a presence list omitting all instance-B users to every client. Multi-instance operation is explicitly intended: docstrings at lines 57 and 81-84 claim cross-instance fan-out, and connection IDs are made unique across instances (lines 327-329). No Redis-backed presence store or merge exists, so presence is wrong whenever more than one instance runs. Medium severity is correct: messaging still works; only presence is corrupted and only in multi-instance deployments, which the code claims to support.

**Fix:** Keep presence in Redis (e.g. a hash/set per project with connection TTLs) so get_presence reflects all instances, or document that presence is only correct in single-instance deployments.

#### `websocket/__init__.py:339` — Clients can forge system message types incl. presence updates

**Status: fixed in 0.33.1.**

`handle_connection` broadcasts client JSON verbatim after stamping `sender_id`/`sender_name`/`timestamp`. Nothing prevents a room member from sending `{"type": "presence", "action": "leave", "user_id": "<victim>", "presence": [...]}` -- the same envelope shape the server itself emits in join_room/leave_room -- so any member can spoof presence changes (or any other server-originated message type the frontend trusts) for everyone in the room. All recipients are authorized room members, so this is integrity rather than confidentiality, but the server gives clients no way to distinguish authentic presence events from forged ones.

*Verifier:* Confirmed in code. src/metaseed_hub/websocket/__init__.py lines 334-348: handle_connection broadcasts client JSON verbatim, stamping only sender_id/sender_name/timestamp, with no restriction on the client-controlled "type" field. The server emits its own presence events (lines 224-233, 250-259) through the same broadcast_to_room path with no authenticity marker. The frontend (src/metaseed_hub/ui/static/js/hub.js lines 186-224) dispatches solely on data.type and updates the presence list from forgeable user_id/user_name/action fields without checking any distinguishing field (it never inspects sender_id), so a room member can spoof presence joins/leaves for any user, and can also forge "update" messages that trigger HTMX refreshes on all clients. The websocket endpoint (main.py lines 155-195) authenticates and authorizes room membership, so attacker and recipients are all project members — integrity-only, matching the finding. Medium severity is correct.

**Fix:** Reserve server-originated types: reject or re-type client messages whose "type" collides with server events (e.g. wrap client payloads as {"type": "chat", ...}), or validate incoming messages against an allowlist of client message types.

### Low (4)

#### `auth/__init__.py:312` — Personal-access-token users silently get empty entitlements

The PAT branch of `get_current_user` constructs `TokenUser(sub=..., roles=[])` and lets `entitlements` default to []. The empty `roles` is deliberate and commented; the empty `entitlements` is neither. Feature grants (`has_feature`/`enabled_features`) key off `TokenUser.entitlements`, so any entitlement-gated capability ever exposed through a Bearer/PAT route will return 404 for a PAT caller even though the same person passes the gate in a browser. Today the only `get_current_user` consumer (api/datasets.py) is not feature-gated, so the gap is latent -- but it is undocumented and will surprise whoever first gates an API route.

*Verifier:* Verified in src/metaseed_hub/auth/__init__.py: the PAT branch (lines 312-319) builds TokenUser with a commented roles=[] while entitlements silently defaults to [] (dataclass default, line 33); the User row from authenticate_token (tokens.py) stores no group URNs, and the OIDC branch of the same dependency does populate entitlements via userinfo, so the asymmetry is real and undocumented. Feature gating genuinely keys off TokenUser.entitlements and returns 404 (ui/dependencies.py:440, features.py:29-56). The latency claim also checks out: the only get_current_user consumers are api/datasets.py routes, none feature-gated, and require_feature is wired to the cookie-session require_user, so no PAT route is gated today. Real consistency/documentation gap, but with no current misbehavior and a comment-level fix, severity is low rather than medium.

**Fix:** Extend the existing comment to cover entitlements (a PAT has no IdP claims, so entitlement-gated features are intentionally unavailable to it), or persist the user's group URNs at sign-in and load them here if PATs are meant to inherit feature grants.

#### `scripts/seed_ontology_demo.py:47` — Demo spec uses ontology_term where the picker filter key is ontologies

Every ontology field in ONTOLOGY_DEMO_SPEC restricts lookup via e.g. `"ontology_term": "ncbitaxon"`. In metaseed's FieldSpec, `ontology_term` records what the field *means* (a term accession like "MIAPPE:DM-1"); the key that scopes the value picker is `ontologies` (a list of ontology ids) — see metaseed/src/metaseed/specs/schema.py FieldSpec docstring, and the hub UI keys the autocomplete off `field.ontologies` only (src/metaseed_hub/ui/templates/partials/entity_form.html: `data-ontologies="{{ field.ontologies | join(',') }}"`, placeholder "Search all ontologies..." otherwise). So every field this script seeds renders an unfiltered "Search all ontologies..." picker, defeating the script's stated purpose (per-ontology filtered lookup: NCBI Taxonomy, PO, ChEBI, ENVO, PATO, UO) and making `any_ontology_term` ("no filter") indistinguishable from the rest.

*Verifier:* Confirmed from code. scripts/seed_ontology_demo.py sets "ontology_term": "ncbitaxon"/"po"/"chebi"/"envo"/"pato"/"uo" (lines 46-81) and never sets "ontologies". FieldSpec (metaseed/src/metaseed/specs/schema.py lines 166-173, 226-227) defines ontology_term as the term the field *means* and ontologies as the picker-scoping list, with absent meaning "any ontology". The consuming chain filters only on ontologies: helper.field_info (metaseed/src/metaseed/facade/helper.py lines 275-278) exposes the keys separately with no fallback; the hub form context (metaseed-hub/src/metaseed_hub/ui/helpers/tables.py line 35) reads info.get("ontologies") only; entity_form.html (lines 133, 140) sets data-ontologies and the filtered placeholder only from field.ontologies, else "Search all ontologies..."; spec-builder.js (lines 113-118) reads dataset.ontologies || dataset.ontologyFilter, never ontology_term. So every seeded field renders an unfiltered picker, making the demo's per-ontology filtering claims false and any_ontology_term indistinguishable from the rest. Severity lowered to low: the defect is real and defeats the script's purpose, but it is a developer seed/demo script with no production or data impact, and it fails soft (lookup still works, just unfiltered).

**Fix:** Replace `"ontology_term": "ncbitaxon"` with `"ontologies": ["ncbitaxon"]` (and likewise for po, chebi, envo, pato, uo).

#### `ui/routes/sharing.py:63` — _db_user_id is never called

`async def _db_user_id(session: DbSession, user: TokenUser) -> str` is defined but referenced nowhere in src/ or tests/ (verified by grep across the repository; the only hit is its definition). _viewer supersedes it - it returns the same db_user id plus the tenant id. Project rules treat dead code as a defect to remove.

*Verifier:* Verified by grep across /Users/sdrwacker/workspace/metaseed-project/metaseed-hub: the only occurrence of _db_user_id in the repo is its definition at src/metaseed_hub/ui/routes/sharing.py:63. It is a module-private (underscore-prefixed) async helper, so it cannot be a FastAPI route or an intended public API, and no string reference exists that could indicate dynamic use. The adjacent _viewer helper (lines 70-75) performs the identical ensure_tenant_and_user call and returns the same db_user id plus the tenant id, and is the helper actually used by _seen_or_404 and the routes. _db_user_id is therefore dead code, which the project's stated rules require removing. Severity corrected to low: it has no runtime or correctness impact, only a code-hygiene cost.

**Fix:** Delete _db_user_id.
